# 10 Static Repeatability

clear()
filename = {sysSequenceFilename}

movel_rel_part(0.025,0.100,0,0,0,0)

gocator_ID=static_pose

count=5
loop:
gocator_trigger(1000)
gocator_write_data({filename})
count--
jump_gt_zero(count,loop)

movel_rel_part({origin})
