# jump

label1:
a=1

label2:
a=2

jump(_label10)
junk to skip
_label10:     #  here we are

jumpif(True,skipit)
junk to skip
skipit:
jumpif(False,junkcode)

jump(label_10)

junkcode:
hasgdj

label_10:

jump(_label_10_)
_label_10_:

jump(a)
a:

jump(xyz) # should fail

jump(zzz)
a=3

zzz:
# Another comment

b=0
a+=2
loop:
a--
b++
jump_gt_zero(a,loop)
assertEqual(b,4)

jump_gt_zero(aa,loop)  # should fail
jump_gt_zero(a,looop)  # should fail

using_java()
idx=3
jump('jlabel')
junk to skip
jlabel:
le_print('java idx=' + idx)
idx--
jumpif(idx>0,'jlabel')

using_python()
idxp = 3
jump('plabel')
junk to skip
plabel:
le_print('python idxp=' + str(idxp))
idxp -= 1
jumpif(idxp>0,'plabel')
